# MiniShop - Документация

Универсальный шаблон Telegram Mini App магазина с интеграцией LEADTEX CRM.

---

## Содержание

1. [Архитектура](#архитектура)
2. [Структура файлов](#структура-файлов)
3. [Конфигурация](#конфигурация)
4. [API модулей](#api-модулей)
5. [Кастомизация](#кастомизация)
6. [Деплой](#деплой)

---

## Архитектура

```
┌─────────────────────────────────────────────────────────────┐
│                     TELEGRAM MINI APP                        │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐    │
│  │ Каталог  │→ │  Товар   │→ │ Корзина  │→ │ Чекаут   │    │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘    │
│       ↓                            ↓             ↓          │
│  ┌──────────┐              ┌──────────────────────┐        │
│  │ config.js│              │     localStorage     │        │
│  │ (данные) │              │   (корзина: cart)    │        │
│  └──────────┘              └──────────────────────┘        │
│                                      ↓                      │
│                            ┌──────────────────────┐        │
│                            │    /api/webhook      │        │
│                            │   (Vercel Proxy)     │        │
│                            └──────────────────────┘        │
│                                      ↓                      │
└─────────────────────────────────────────────────────────────┘
                                       ↓
                            ┌──────────────────────┐
                            │     LEADTEX CRM      │
                            │  (inner_webhook)     │
                            └──────────────────────┘
```

### Поток данных

1. **Каталог** → Пользователь просматривает товары
2. **Товар** → Пользователь выбирает товар, указывает количество
3. **Корзина** → Товары сохраняются в `localStorage`
4. **Чекаут** → Пользователь заполняет форму
5. **Отправка** → POST запрос на `/api/webhook`
6. **Vercel** → Проксирует запрос в LEADTEX
7. **LEADTEX** → Обрабатывает заказ, запускает сценарии

---

## Структура файлов

```
keychain-shop/
├── index.html              # Главная страница (SPA)
├── css/
│   └── style.css           # Стили приложения
├── js/
│   ├── config.js           # Конфигурация магазина
│   ├── telegram.js         # Telegram Web App API
│   ├── cart.js             # Управление корзиной
│   └── app.js              # Основная логика
├── api/
│   └── webhook.js          # Vercel Serverless Function
├── vercel.json             # Конфигурация Vercel
├── DOCS.md                 # Эта документация
├── LEADTEX_INTEGRATION.md  # Инструкция по интеграции
└── tests/
    └── *.test.js           # Тесты
```

---

## Конфигурация

Вся конфигурация находится в `js/config.js`.

### CONFIG.WEBHOOK_URL

```javascript
WEBHOOK_URL: '/api/webhook'
```

URL для отправки заказов. По умолчанию использует Vercel proxy.

### CONFIG.SHOP

```javascript
SHOP: {
    name: 'MiniShop',       // Название магазина
    logo: '🛍️',             // Эмодзи логотипа
    currency: '₽',          // Символ валюты
    currencyCode: 'RUB'     // ISO код валюты
}
```

### CONFIG.CATEGORIES

```javascript
CATEGORIES: [
    { id: 'all', name: 'Все товары', icon: '🏠' },
    { id: 'electronics', name: 'Электроника', icon: '📱' },
    // ...
]
```

| Поле | Тип | Описание |
|------|-----|----------|
| `id` | string | Уникальный идентификатор категории |
| `name` | string | Отображаемое название |
| `icon` | string | Эмодзи иконка |

### CONFIG.PRODUCTS

```javascript
PRODUCTS: [
    {
        id: 'product-001',
        name: 'Название товара',
        price: 990,
        oldPrice: 1490,           // Опционально
        category: 'electronics',
        badge: 'Хит продаж',      // Опционально
        rating: 4.9,
        reviews: 127,
        image: 'https://...',
        images: ['url1', 'url2'], // Галерея
        description: 'Описание...',
        features: [
            { label: 'Материал', value: 'Сталь' }
        ],
        inStock: true
    }
]
```

| Поле | Тип | Обязательно | Описание |
|------|-----|-------------|----------|
| `id` | string | Да | Уникальный ID товара |
| `name` | string | Да | Название |
| `price` | number | Да | Цена |
| `oldPrice` | number | Нет | Старая цена (для скидки) |
| `category` | string | Да | ID категории |
| `badge` | string | Нет | Бейдж (Хит, Новинка, -20%) |
| `rating` | number | Да | Рейтинг 1-5 |
| `reviews` | number | Да | Количество отзывов |
| `image` | string | Да | URL главного изображения |
| `images` | array | Да | Массив URL для галереи |
| `description` | string | Да | Описание товара |
| `features` | array | Да | Характеристики |
| `inStock` | boolean | Да | В наличии |

### CONFIG.DELIVERY

```javascript
DELIVERY: {
    freeShippingThreshold: 2000,  // Бесплатная доставка от
    cost: 299,                     // Стоимость доставки
    estimatedDays: '1-3'          // Срок доставки
}
```

---

## API модулей

### config.js

#### `CONFIG.getProductById(id)`

Возвращает товар по ID.

```javascript
const product = CONFIG.getProductById('product-001');
// { id: 'product-001', name: '...', ... }
```

#### `CONFIG.getProductsByCategory(categoryId)`

Возвращает товары категории.

```javascript
const products = CONFIG.getProductsByCategory('electronics');
// [{ id: '...', ... }, ...]

const allProducts = CONFIG.getProductsByCategory('all');
// Все товары
```

#### `CONFIG.formatPrice(price)`

Форматирует цену с валютой.

```javascript
CONFIG.formatPrice(1990);
// "1 990 ₽"
```

#### `CONFIG.getDiscount(price, oldPrice)`

Рассчитывает скидку в процентах.

```javascript
CONFIG.getDiscount(990, 1490);
// 34
```

---

### cart.js

Класс `Cart` управляет корзиной.

#### `cart.add(product, quantity)`

Добавляет товар в корзину.

```javascript
cart.add({ id: '001', name: 'Товар', price: 990, image: '...' }, 2);
```

#### `cart.remove(productId)`

Удаляет товар из корзины.

```javascript
cart.remove('001');
```

#### `cart.updateQuantity(productId, quantity)`

Обновляет количество товара.

```javascript
cart.updateQuantity('001', 5);
```

#### `cart.getItems()`

Возвращает массив товаров в корзине.

```javascript
const items = cart.getItems();
// [{ id: '001', name: '...', quantity: 2, ... }]
```

#### `cart.getCount()`

Возвращает общее количество товаров.

```javascript
cart.getCount(); // 5
```

#### `cart.getTotal()`

Возвращает сумму без доставки.

```javascript
cart.getTotal(); // 4950
```

#### `cart.getDeliveryCost()`

Возвращает стоимость доставки.

```javascript
cart.getDeliveryCost(); // 0 или CONFIG.DELIVERY.cost
```

#### `cart.getFinalTotal()`

Возвращает итоговую сумму с доставкой.

```javascript
cart.getFinalTotal(); // 4950
```

#### `cart.clear()`

Очищает корзину.

```javascript
cart.clear();
```

#### `cart.isEmpty()`

Проверяет, пуста ли корзина.

```javascript
cart.isEmpty(); // true/false
```

---

### telegram.js

Класс `TelegramApp` работает с Telegram Web App API.

#### `telegramApp.getUserId()`

Возвращает Telegram ID пользователя.

```javascript
telegramApp.getUserId(); // 123456789
```

#### `telegramApp.getUserName()`

Возвращает имя пользователя.

```javascript
telegramApp.getUserName(); // "Иван Петров"
```

#### `telegramApp.showAlert(message)`

Показывает алерт.

```javascript
telegramApp.showAlert('Товар добавлен!');
```

#### `telegramApp.showConfirm(message, callback)`

Показывает диалог подтверждения.

```javascript
telegramApp.showConfirm('Удалить?', (confirmed) => {
    if (confirmed) { /* ... */ }
});
```

#### `telegramApp.hapticFeedback(type)`

Вибрация устройства.

```javascript
telegramApp.hapticFeedback('light');   // Легкая
telegramApp.hapticFeedback('medium');  // Средняя
telegramApp.hapticFeedback('success'); // Успех
telegramApp.hapticFeedback('error');   // Ошибка
```

---

### app.js

Основные функции приложения.

#### Навигация

```javascript
showCatalog();    // Показать каталог
showCart();       // Показать корзину
showCheckout();   // Показать оформление
openProduct(id);  // Открыть страницу товара
```

#### Категории

```javascript
selectCategory('electronics'); // Выбрать категорию
renderCategories();            // Отрисовать категории
renderProducts();              // Отрисовать товары
```

#### Корзина

```javascript
addToCart();                  // Добавить текущий товар
removeFromCart(productId);    // Удалить из корзины
renderCart();                 // Отрисовать корзину
```

#### Заказ

```javascript
submitOrder(event);           // Отправить заказ
sendToLeadtex(orderData);     // Отправить в LEADTEX
```

---

## Кастомизация

### Изменение темы

В `css/style.css` измените CSS-переменные:

```css
:root {
    --primary-color: #0088cc;     /* Основной цвет */
    --primary-dark: #006699;      /* Темный основной */
    --secondary-color: #ff6b6b;   /* Акцентный цвет */
    --success-color: #51cf66;     /* Цвет успеха */
    --warning-color: #ffd93d;     /* Цвет предупреждения */
    --text-primary: #2c3e50;      /* Основной текст */
    --text-secondary: #7f8c8d;    /* Второстепенный текст */
    --bg-light: #f8f9fa;          /* Светлый фон */
    --bg-white: #ffffff;          /* Белый фон */
    --border-color: #e1e8ed;      /* Цвет границ */
    --radius-sm: 8px;             /* Малый радиус */
    --radius-md: 12px;            /* Средний радиус */
    --radius-lg: 16px;            /* Большой радиус */
}
```

### Добавление нового товара

В `config.js` добавьте объект в массив `PRODUCTS`:

```javascript
{
    id: 'unique-id-007',
    name: 'Новый товар',
    price: 1990,
    oldPrice: 2990,
    category: 'electronics',
    badge: 'Новинка',
    rating: 4.8,
    reviews: 0,
    image: 'https://images.unsplash.com/...',
    images: [
        'https://images.unsplash.com/...',
        'https://images.unsplash.com/...'
    ],
    description: 'Описание нового товара...',
    features: [
        { label: 'Характеристика', value: 'Значение' }
    ],
    inStock: true
}
```

### Добавление новой категории

В `config.js` добавьте объект в массив `CATEGORIES`:

```javascript
{ id: 'new-category', name: 'Новая категория', icon: '🆕' }
```

Затем укажите `category: 'new-category'` у товаров.

---

## Деплой

### Vercel (рекомендуется)

1. Создайте репозиторий на GitHub
2. Подключите к Vercel: https://vercel.com/new
3. Vercel автоматически обнаружит `vercel.json`
4. Деплой происходит автоматически при push

### Настройка вебхука

В `vercel.json` измените destination на ваш LEADTEX webhook:

```json
{
  "rewrites": [
    {
      "source": "/api/webhook",
      "destination": "https://YOUR_LEADTEX_URL/inner_webhook/YOUR_UUID"
    }
  ]
}
```

### Подключение к Telegram боту

1. Создайте бота через @BotFather
2. Отправьте команду `/newapp`
3. Укажите URL вашего Vercel деплоя
4. Добавьте кнопку Mini App в бота

---

## Переменные окружения

Для локальной разработки создайте `.env`:

```env
# Включить режим разработки
DEV_MODE=true
```

В `config.js` установите `DEV_MODE: true` для тестирования без Telegram.

---

## Лицензия

MIT License
